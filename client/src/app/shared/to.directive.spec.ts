import { ToDirective } from './to.directive';

describe('ToDirective', () => {
  it('should create an instance', () => {
    const directive = new ToDirective();
    expect(directive).toBeTruthy();
  });
});
