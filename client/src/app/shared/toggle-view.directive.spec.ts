import { ToggleViewDirective } from './toggle-view.directive';

describe('ToggleViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleViewDirective();
    expect(directive).toBeTruthy();
  });
});
